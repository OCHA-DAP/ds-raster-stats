azure-storage-blob==12.20.0
adlfs==2024.4.1
xarray==2024.3.0
rioxarray==0.16.0
dask==2024.7.0
Jinja2==3.1.4
tqdm==4.66.4
rasterstats==0.19.0
geopandas==1.0.1
coloredlogs==15.0.1
python-dotenv==1.0.1
psycopg2_binary==2.9.9
sqlalchemy==2.0.33
rasterio==1.3.10
pytest==8.3.3
numpy<2.0.0
pandas==2.2.3
exactextract==0.3.0
# scipy >=1.16 requires Python >=3.11; the Databricks runtime is
# 3.10, so cap it to keep `pip install -r` working there.
scipy==1.15.3
# affine >=2.4 puts a cached_property on a __slots__ namedtuple, which
# raises on Python 3.10 (e.g. Databricks runtime 14.3) whenever a
# rasterio transform is indexed. Fine on 3.12+, so pin for portability.
affine==2.3.1
